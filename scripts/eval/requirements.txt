# Polish Academic MCP — Python Evaluation Framework
# Install: pip install -r scripts/eval/requirements.txt

# Core
httpx>=0.27.0           # Async HTTP client for MCP SSE/HTTP
pydantic>=2.0.0         # Data validation
rich>=13.0.0            # Terminal output formatting
python-dotenv>=1.0.0    # .env file loading

# MCP client
mcp>=1.0.0              # Model Context Protocol Python SDK

# CUDA batch evaluation (optional — install separately if GPU available)
# torch>=2.0.0          # pip install torch --index-url https://download.pytorch.org/whl/cu121
# transformers>=4.35.0  # For local LLM inference
# accelerate>=0.25.0    # Distributed training / inference

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
